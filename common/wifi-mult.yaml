wifi:
  networks:
  - ssid: $wifi_ssid
    password: $wifi_password
  - ssid: $wifi_ssid_alt
    password: $wifi_password_alt

  # Enable fallback hotspot (captive portal) in case wifi connection fails
  ap:
    ssid: "$devicename"
    password: $ap_fallback_password

captive_portal: